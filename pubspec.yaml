name: ttt
description: "tic tac to game!"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.1.4
  flutter_bloc: ^8.1.6
  intl: ^0.19.0
  very_good_analysis: ^6.0.0
  mix: ^1.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.12

flutter:
  uses-material-design: true
